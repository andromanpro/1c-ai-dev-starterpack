# Практики ИИ-разработки в 1С

> Рабочее название. Статус: **в сборке, первый выпуск не опубликован.**

Порядок безопасной работы 1С-разработчика с ИИ-агентами: как поставить задачу,
как проверить результат, когда не верить агенту и что делать, когда врёт сам
инструмент. Это **не** набор инструментов — инструментальные операции берутся
из соседних проектов (ссылки в справочнике); здесь — порядок работы и проверки.

## Для кого

Опытный 1С-ник, новичок в работе с ИИ. Через 15 минут — агент подключён и выполнил
первую безопасную задачу (анализ, без правок). Через 60–90 минут — первое проверенное
изменение. За первую неделю — самостоятельный повседневный цикл: постановка,
проверка, приёмка, самопочинка инструментов (страницы — в `docs/`).

## До / После

| Без набора | С набором |
|---|---|
| Агент правит что-то где-то; проверка — «вроде работает» | Первая задача — всегда анализ; правки — после «да»; продуктив запрещён правилом, а не памятью |
| «Готово» агента принимается на слово | «Готово» подтверждается контрольной точкой, которая была красной до правки |
| Инструмент сломался → обход или ожидание вендора | Дефект → воспроизведение → починка агентом → проверка → исправление уходит в исходный проект |
| Контекст пересказывается каждой новой сессии | Контекст в файлах: стартер + «продолжаем» |
| «Подойдёт ли дешёвая модель?» — гадание | Профиль «слабая модель» + протокол испытаний с вердиктами |

## С чего начать

1. Склонируйте набор, откройте его своей агентской средой.
2. Пройдите [быстрый старт](docs/быстрый-старт.md) — две приёмки на стенде.
3. Скажите агенту «настрой окружение» — сценарии `install/` он исполнит сам.

## Три принципа

1. **Инструмент, который врёт — это задача, а не препятствие.** Ошибку инструмента
   чиним через ИИ в исходнике и отдаём исправление в исходный проект, а не обходим.
2. **Проверка вынесена из агента наружу.** Три контура: вторая модель, машинные
   контрольные точки (тесты, АПК, Sonar), глаза человека в тестовой базе. Правило,
   которое надо помнить, ненадёжно — надёжен шлагбаум, который срабатывает до действия.
3. **Контекст живёт в файлах.** Стартеры сессий, реестры, правила — поэтому рабочие
   сообщения агенту сжимаются до «путь + продолжаем» и «цитата + давай».

## Карта разделов (план выпуска)

| Раздел | Что внутри | Состояние |
|---|---|---|
| `docs/` — вики, вход: [docs/README.md](docs/README.md) | Быстрый старт, теория контуров, повседневный цикл, самопочинка, карточки деформаций, справочник, отказы, термины | ✔ есть |
| `стенд/` | Приёмка контура: модуль с дефектами + два приёмочных прогона + контрольная точка с самотестом | ✔ есть |
| `install/` | Сценарии установки, исполняемые агентом: диагностика, EDT+codepilot, линии проверки и тестирования | ✔ 00–03 |
| `docs/` Повседневный цикл | Формы постановки задач, стартеры сессий, три контура проверки, приёмка | ✔ есть |
| `docs/` Самопочинка инструмента | Процедура: дефект → воспроизведение → исправление агентом → проверка → передача в исходный проект | ✔ есть |
| `docs/` Карточки деформаций | Антипаттерн → симптомы → шлагбаум | ✔ есть |
| `docs/` Словарь понятий | Агентская среда ≠ модель, MCP, навык агента, контрольная точка | ✔ есть |
| `docs/` Справочник инструментов | Датированный обзор экосистемы + «что это и когда возьмёшь» | ✔ есть |
| `rules/` | Ядро правил (4 файла) + профиль «слабая модель» + подключаемые справочники: код BSL, 69 антипаттернов, управляемые и обычные формы | ✔ есть |
| `skills/` | Навыки агента: кириллица-в-путях, восстановление файла из журналов сессий | ✔ есть (практика codepilot — в следующей версии) |
| `templates/` | Инструкция проекта, шаблон стартера, проверки перед действием | ✔ есть |
| `tools/` | Проверка перед публикацией (самотест) + генератор фактов стартера | ✔ есть |
| `испытания-моделей/` | Методика и протоколы проверки пригодности моделей (Haiku, DeepSeek — пройдено) | ✔ есть |

## Когда этот набор НЕ подойдёт

- Вы ищете готовые операции над файлами 1С (выгрузки, формы, макеты) — это
  к инструментальным наборам, мы на них ссылаемся, но их не дублируем.
- Вам нужен курс по 1С — здесь 1С-квалификация предполагается.
- Вы не готовы проверять за агентом — набор построен на недоверии по умолчанию,
  без проверки он не работает.

## Лицензия

MIT. Расширение codepilot1c — отдельный проект под AGPL-3.0, сюда не входит,
устанавливается по ссылке из его выпусков.
